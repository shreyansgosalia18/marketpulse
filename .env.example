# Copy to .env to override the local-dev Postgres credentials used by docker-compose.yml.
# Not required — the compose file has working defaults without this file.
POSTGRES_USER=marketpulse
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=marketpulse
